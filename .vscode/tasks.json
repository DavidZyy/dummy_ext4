{
  "tasks": [
    {
      "type": "process",
      "label": "make_compile",
      "command": "make",
      "args": [
        "compile",
        "TEST=${fileBasenameNoExtension}"
      ],
      "options": {
        // 罪魁祸首，执行make的路径搞错了
        // "cwd": "${fileDirname}"
      },
      "problemMatcher": [
        "$gcc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Task generated by Debugger."
    },
  ],
  "version": "2.0.0"
}